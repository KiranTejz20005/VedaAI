# Scaling
Scale async workers and retrieval independently from the application API. Set per-tenant quotas, concurrency limits and model-cost budgets. Cache non-confidential derived metadata, not raw papers. Establish load, latency and cost SLOs before scaling; forecast peak traffic around assessment creation deadlines.
