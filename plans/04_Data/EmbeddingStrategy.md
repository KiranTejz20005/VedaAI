# Embedding strategy
Chunk by meaningful instructional sections, retain headings and page references, and embed only text authorised for retrieval. Namespace vectors by tenant and document version. Pair dense similarity with lexical search, metadata filters and reranking. Re-embed deliberately on model or chunking changes; preserve old index versions until regression checks pass.
