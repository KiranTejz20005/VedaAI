# RAG architecture
Index only approved, tenant-scoped curriculum sources. Retrieve by course, unit, outcome, topic and document version; hybrid-search for keywords plus embeddings; rerank; pass compact cited context to the model. Reject a draft when sufficient approved evidence is absent. Keep retrieval snapshots with generated artefacts for auditability.
